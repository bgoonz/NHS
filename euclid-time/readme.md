euclid-time generates beat arrays which can be used for timing algorithms in anither module called zerone.

module exports a function(n, m) where n < m, returning an array length n + m sorted euclid-wise.
