spawn = require('child_process').spawn
err = function(d){
console.log(d.toString())
}}
err = function(d){
console.log(d.toString())
}
baud = spawn('baudline', ['-debugmeasure', '-format', 's8']); baud.stdout.on('error', err); 
baud = spawn('baudline', ['-debugmeasure', '-format', 's8']); baud.stdout.on('error', err); 
baud.kill()
baud.killed
baud.kill()
baud.kill()
baud = spawn('baudline', ['-debugmeasure', '-format', 's8']); baud.stdout.on('error', err); 
baud = spawn('baudline', ['-debugmeasure', '-format', 's8', '-debugtimer', '1000']); baud.stdout.on('error', err); 
